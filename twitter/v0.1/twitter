function url_replace(url, args, param, type, required, def) {
	if (args[param] === undefined && required && def !== undefined)
		args[param] = def;
	if (args[param] === undefined && required && def === undefined)
		throw new Error(param + ' is a required parameter');
	if (type == 'str' && args[param] !== undefined && args[param] !== undefined && args[param] !== null && typeof args[param] != 'string')
		args[param] = args[param].toString();
	if (type == 'int' && args[param] !== undefined && (parseFloat(args[param]) != parseInt(args[param], 10) || isNaN(args[param])))
		throw new Error(param + ' must be an integer');
	if (url.indexOf('{' + param + '}') !== -1) {
		var rgx = new RegExp('\\{' + param + '\\}', 'g');
		url = url.replace(rgx, args[param]);
		delete args[param];
	}
	return url;
}

var provider = fs('/bin/twitter');

exports.auth = function() { return auth({provider:provider}) };
/** 
 * Returns a collection of the most recent Tweets and retweets posted by the authenticating user and the users they follow. The home timeline is central to how most users interact with the Twitter service.               Up to 800 Tweets are obtainable on the home timeline. It is more volatile for users that follow many users or follow users who tweet frequently.
 * @category Timeline
 * @param {string} [format=json]  undefined
 * @param {string}  count   Specifies the number of records to retrieve. Must be less than or equal to 200. Defaults to 20.
 * @param {string}  max_id   Returns results with an ID less than (that is, older than) or equal to the specified ID.
 * @param {string}  since_id   Returns results with an ID greater than (that is, more recent than) the specified ID. There are limits to the number of Tweets which can be accessed through the API. If the limit of Tweets has occured since the since_id, the since_id will be forced to the oldest ID available.
 * @param {string}  trim_user   When set to either true, t or 1, each tweet returned in a timeline will include a user object including only the status authors numerical ID. Omit this parameter to receive the complete user object.
 * @param {boolean}  exclud_replies   This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets — this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
 * @param {boolean}  contributor_details   This parameter enhances the contributors element of the status response to include the screen_name of the contributor. By default only the user_id of the contributor is included.
 * @param {string}  include_entities   The entities node will be disincluded when set to false.
 * @methodhttp GET
 */
exports.homeTimeline = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/home_timeline.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'count', 'str');
	url = url_replace(url, args, 'max_id', 'str');
	url = url_replace(url, args, 'since_id', 'str');
	url = url_replace(url, args, 'trim_user', 'str');
	url = url_replace(url, args, 'exclud_replies');
	url = url_replace(url, args, 'contributor_details');
	url = url_replace(url, args, 'include_entities', 'str', false, "true");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * API version 1.1               Updated on Mon, 2012-10-15 07:23               Returns a collection of the most recent Tweets posted by the user indicated by the screen_name or user_id parameters.               User timelines belonging to protected users may only be requested when the authenticated user either "owns" the timeline or is an approved follower of the owner.               The timeline returned is the equivalent of the one seen when you view a user's profile on twitter.com.               This method can only return up to 3,200 of a user's most recent Tweets. Native retweets of other statuses by the user is included in this total, regardless of whether include_rts is set to false when requesting this resource.
 * @category Timeline
 * @param {string} [format=json]  undefined
 * @param {integer}  count   Specifies the number of tweets to try and retrieve, up to a maximum of 200 per distinct request. The value of count is best thought of as a limit to the number of tweets to return because suspended or deleted content is removed after the count has been applied. We include retweets in the count, even if include_rts is not supplied. It is recommended you always send include_rts=1 when using this API method.
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name. Specifies the ID of the user to befriend. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {integer}  since_id   Returns results with an ID greater than (that is, more recent than) the specified ID. There are limits to the number of Tweets which can be accessed through the API. If the limit of Tweets has occured since the since_id, the since_id will be forced to the oldest ID available.
 * @param {integer}  max_id   Returns results with an ID less than (that is, older than) or equal to the specified ID.
 * @param {string}  trim_user   When set to either true, t or 1, each tweet returned in a timeline will include a user object including only the status authors numerical ID. Omit this parameter to receive the complete user object.
 * @param {boolean}  exclude_replies   This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets — this is because the count parameter retrieves that many tweets before filtering out retweets and replies. This parameter is only supported for JSON and XML responses.
 * @param {boolean}  contributor_details   This parameter enhances the contributors element of the status response to include the screen_name of the contributor. By default only the user_id of the contributor is included.
 * @param {boolean}  include_rts   When set to false, the timeline will strip any native retweets (though they will still count toward both the maximal length of the timeline and the slice selected by the count parameter). Note: If you're using the trim_user parameter in conjunction with include_rts, the retweets will still contain a full user object.
 * @methodhttp GET
 */
exports.userTimeline = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/user_timeline.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'count', 'int');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'since_id', 'int');
	url = url_replace(url, args, 'max_id', 'int');
	url = url_replace(url, args, 'trim_user', 'str');
	url = url_replace(url, args, 'exclude_replies');
	url = url_replace(url, args, 'contributor_details');
	url = url_replace(url, args, 'include_rts');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the 20 most recent mentions (tweets containing a users's @screen_name) for the authenticating user.               The timeline returned is the equivalent of the one seen when you view your mentions on twitter.com.               This method can only return up to 800 tweets
 * @category Timeline
 * @param {string} [format=json]  undefined
 * @param {integer}  count   Specifies the number of tweets to try and retrieve, up to a maximum of 200. The value of count is best thought of as a limit to the number of tweets to return because suspended or deleted content is removed after the count has been applied. We include retweets in the count, even if include_rts is not supplied. It is recommended you always send include_rts=1 when using this API method.
 * @param {integer}  max_id   Returns results with an ID less than (that is, older than) or equal to the specified ID.
 * @param {integer}  since_id   Returns results with an ID greater than (that is, more recent than) the specified ID. There are limits to the number of Tweets which can be accessed through the API. If the limit of Tweets has occured since the since_id, the since_id will be forced to the oldest ID available.
 * @param {boolean}  include_entities   The entities node will be disincluded when set to false.
 * @param {boolean}  contributor_details   This parameter enhances the contributors element of the status response to include the screen_name of the contributor. By default only the user_id of the contributor is included.
 * @methodhttp GET
 */
exports.mentionsTimeline = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/mentions_timeline.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'count', 'int');
	url = url_replace(url, args, 'max_id', 'int');
	url = url_replace(url, args, 'since_id', 'int');
	url = url_replace(url, args, 'include_entities');
	url = url_replace(url, args, 'contributor_details');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the most recent tweets authored by the authenticating user that have recently been retweeted by others. This timeline is a subset of the user's GET statuses/user_timeline.
 * @category Timeline
 * @param {string} [format=json]  undefined
 * @param {integer}  count   Specifies the number of tweets to try and retrieve, up to a maximum of 200. The value of count is best thought of as a limit to the number of tweets to return because suspended or deleted content is removed after the count has been applied. We include retweets in the count, even if include_rts is not supplied. It is recommended you always send include_rts=1 when using this API method.
 * @param {integer}  max_id   Returns results with an ID less than (that is, older than) or equal to the specified ID.
 * @param {integer}  since_id   Returns results with an ID greater than (that is, more recent than) the specified ID. There are limits to the number of Tweets which can be accessed through the API. If the limit of Tweets has occured since the since_id, the since_id will be forced to the oldest ID available.
 * @param {boolean}  include_entities   The entities node will be disincluded when set to false.
 * @param {boolean}  include_user_entities   The user entities node will be disincluded when set to false.
 * @methodhttp GET
 */
exports.retweetsOfMe = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/retweets_of_me.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'count', 'int');
	url = url_replace(url, args, 'max_id', 'int');
	url = url_replace(url, args, 'since_id', 'int');
	url = url_replace(url, args, 'include_entities');
	url = url_replace(url, args, 'include_user_entities');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a single Tweet, specified by the id parameter. The Tweet's author will also be embedded within the tweet.
 * @category Tweets
 * @param {string} [format=json]  undefined
 * @param {integer} [id]  The numerical ID of the desired Tweet.
 * @param {string}  trim_user   When set to either true, t or 1, each tweet returned in a timeline will include a user object including only the status authors numerical ID. Omit this parameter to receive the complete user object.
 * @param {string}  include_my_retweet   When set to either true, t or 1, any Tweets returned that have been retweeted by the authenticating user will include an additional current_user_retweet node, containing the ID of the source status for the retweet.
 * @param {boolean}  include_entities   The entities node will be disincluded when set to false.
 * @methodhttp GET
 */
exports.showStatus = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/show/{id}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'int', true);
	url = url_replace(url, args, 'trim_user', 'str');
	url = url_replace(url, args, 'include_my_retweet', 'str');
	url = url_replace(url, args, 'include_entities');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Updates the authenticating user's current status, also known as tweeting. To upload an image to accompany the tweet, use POST statuses/update_with_media.                       For each update attempt, the update text is compared with the authenticating user's recent tweets. Any attempt that would result in duplication will be blocked, resulting in a 403 error. Therefore, a user cannot submit the same status twice in a row.                       While not rate limited by the API a user is limited in the number of tweets they can create at a time. If the number of updates posted by the user reaches the current allowed limit this method will return an HTTP 403 error.
 * @category Tweets
 * @param {string} [format=json]  undefined
 * @param {string} [status]  The text of your status update, typically up to 140 characters. URL encode as necessary. t.co link wrapping may effect character counts.                There are some special commands in this field to be aware of. For instance, preceding a message with "D " or "M " and following it with a screen name can create a direct message to that user if the relationship allows for it.
 * @param {string}  place_id   A place in the world. These IDs can be retrieved from apis.twitter.reverseGeocode.
 * @param {integer}  long   The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This parameter will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding lat parameter.
 * @param {integer}  in_reply_to_status_id   The ID of an existing status that the update is in reply to.                    Note:: This parameter will be ignored unless the author of the tweet this parameter references is mentioned within the status text. Therefore, you must include @username, where username is the author of the referenced tweet, within the update.
 * @param {boolean}  display_coordinates   Whether or not to put a pin on the exact coordinates a tweet has been sent from.
 * @param {integer}  lat   The latitude of the location this tweet refers to. This parameter will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding long parameter.
 * @param {string}  trim_user   When set to either true, t or 1, each tweet returned in a timeline will include a user object including only the status authors numerical ID. Omit this parameter to receive the complete user object.
 * @methodhttp POST
 */
exports.tweet = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/update.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'status', 'str', true);
	url = url_replace(url, args, 'place_id', 'str');
	url = url_replace(url, args, 'long', 'int');
	url = url_replace(url, args, 'in_reply_to_status_id', 'int');
	url = url_replace(url, args, 'display_coordinates');
	url = url_replace(url, args, 'lat', 'int');
	url = url_replace(url, args, 'trim_user', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Destroys the status specified by the required ID parameter. The authenticating user must be the author of the specified status. Returns the destroyed status if successful.
 * @category Tweets
 * @param {string} [format=json]  undefined
 * @param {integer} [id]  The numerical ID of the desired status.
 * @param {string}  trim_user   When set to either true, t or 1, each tweet returned in a timeline will include a user object including only the status authors numerical ID. Omit this parameter to receive the complete user object.
 * @methodhttp POST
 */
exports.destroyStatus = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/destroy/{id}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'int', true);
	url = url_replace(url, args, 'trim_user', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Retweets a tweet. Returns the original tweet with retweet details embedded.
 * @category Tweets
 * @param {string} [format=json]  undefined
 * @param {integer} [id]  The numerical ID of the desired status.
 * @param {string}  trim_user   When set to either true, t or 1, each tweet returned in a timeline will include a user object including only the status authors numerical ID. Omit this parameter to receive the complete user object.
 * @methodhttp POST
 */
exports.retweet = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/retweet/{id}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'int', true);
	url = url_replace(url, args, 'trim_user', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns up to 100 of the first retweets of a given tweet.
 * @category Tweets
 * @param {string} [format=json]  undefined
 * @param {integer} [id]  The numerical ID of the desired status.
 * @param {integer}  count   Specifies the number of records to retrieve. Must be less than or equal to 100.
 * @param {string}  trim_user   When set to either true, t or 1, each tweet returned in a timeline will include a user object including only the status authors numerical ID. Omit this parameter to receive the complete user object.
 * @methodhttp GET
 */
exports.retweets = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/retweets/{id}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'int', true);
	url = url_replace(url, args, 'count', 'int');
	url = url_replace(url, args, 'trim_user', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Updates the authenticating user's current status and attaches media for upload. In other words, it creates a Tweet with a picture attached.Unlike POST statuses/update, this method expects raw multipart data. Your POST request's Content-Type should be set to multipart/form-data with the media[] parameterThe Tweet text will be rewritten to include the media URL(s), which will reduce the number of characters allowed in the Tweet text. If the URL(s) cannot be appended without text truncation, the tweet will be rejected and this method will return an HTTP 403 error.Important: In API v1.1, you now use api.twitter.com as the domain instead of upload.twitter.com. We strongly recommend using SSL with this method.                  
 * @category Tweets
 * @param {string} [format=json]  undefined
 * @param {string} [status]  The text of your status update. URL encode as necessary. t.co link wrapping may affect character counts if the post contains URLs. You must additionally account for the characters_reserved_per_media per uploaded media, additionally accounting for space characters in between finalized URLs.Note: Request the GET help/configuration endpoint to get the current characters_reserved_per_media and max_media_per_upload values.                   
 * @param {array} [media]  Up to max_media_per_upload files may be specified in the request, each named media[]. Supported image formats are PNG, JPG and GIF. Animated GIFs are not supported. This data must be the raw image bytes - base64 encoded images are currently not supported.Note: Request the GET help/configuration endpoint to get the current max_media_per_upload and photo_size_limit values.                   
 * @param {string}  possibly_sensitive   Set to true for content which may not be suitable for every audience.                   
 * @param {string}  in_reply_to_status_id   The ID of an existing status that the update is in reply to.Note: This parameter will be ignored unless the author of the tweet this parameter references is mentioned within the status text. Therefore, you must include @username, where username is the author of the referenced tweet, within the update.                   
 * @param {string}  lat   The latitude of the location this tweet refers to. This parameter will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding long parameter.Example Values: 37.7821120598956                   
 * @param {string}  long   The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This parameter will be ignored if outside that range, not a number, geo_enabled is disabled, or if there not a corresponding lat parameter.Example Values: -122.400612831116                   
 * @param {string}  place_id   A place in the world identified by a Twitter place ID. Place IDs can be retrieved from geo/reverse_geocode.Example Values: df51dec6f4ee2b2c                   
 * @param {string}  display_coordinates   Whether or not to put a pin on the exact coordinates a tweet has been sent from.Example Values: true                   
 * @methodhttp POST
 */
exports.updateStatusWithMedia = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/update_with_media.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'status', 'str', true);
	url = url_replace(url, args, 'media', 'null', true);
	url = url_replace(url, args, 'possibly_sensitive', 'str');
	url = url_replace(url, args, 'in_reply_to_status_id', 'str');
	url = url_replace(url, args, 'lat', 'str');
	url = url_replace(url, args, 'long', 'str');
	url = url_replace(url, args, 'place_id', 'str');
	url = url_replace(url, args, 'display_coordinates', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns information allowing the creation of an embedded representation of a Tweet on third party sites. See the oEmbed specification for information about the response format.While this endpoint allows a bit of customization for the final appearance of the embedded Tweet, be aware that the appearance of the rendered Tweet may change over time to be consistent with Twitter's Display Requirements. Do not rely on any class or id parameters to stay constant in the returned markup.                  
 * @category Tweets
 * @param {string} [format=json]  undefined
 * @param {string} [id]  The Tweet/status ID to return embed code for.Example Values: 99530515043983360                   
 * @param {string} [url]  The URL of the Tweet/status to be embedded.Example Values:To embed the Tweet at https://twitter.com/#!/twitter/status/99530515043983360, use:https%3A%2F%2Ftwitter.com%2F%23!%2Ftwitter%2Fstatus%2F99530515043983360To embed the Tweet at https://twitter.com/twitter/status/99530515043983360, use:https%3A%2F%2Ftwitter.com%2Ftwitter%2Fstatus%2F99530515043983360                   
 * @param {string}  maxwidth   The maximum width in pixels that the embed should be rendered at. This value is constrained to be between 250 and 550 pixels.Note that Twitter does not support the oEmbed maxheight parameter. Tweets are fundamentally text, and are therefore of unpredictable height that cannot be scaled like an image or video. Relatedly, the oEmbed response will not provide a value for height. Implementations that need consistent heights for Tweets should refer to the hide_thread and hide_media parameters below.Example Values: 325                   
 * @param {string}  hide_media   Specifies whether the embedded Tweet should automatically expand images which were uploaded via POST statuses/update_with_media. When set to either true, t or 1 images will not be expanded. Defaults to false.Example Values: true                   
 * @param {string}  hide_thread   Specifies whether the embedded Tweet should automatically show the original message in the case that the embedded Tweet is a reply. When set to either true, t or 1 the original Tweet will not be shown. Defaults to false.Example Values: true                   
 * @param {string}  omit_script   Specifies whether the embedded Tweet HTML should include a script element pointing to widgets.js. In cases where a page already includes widgets.js, setting this value to true will prevent a redundant script element from being included. When set to either true, t or 1 the script element will not be included in the embed HTML, meaning that pages must include a reference to widgets.js manually. Defaults to false.Example Values: true                   
 * @param {string}  align   Specifies whether the embedded Tweet should be left aligned, right aligned, or centered in the page. Valid values are left, right, center, and none. Defaults to none, meaning no alignment styles are specified for the Tweet.Example Values: center                   
 * @param {string}  related   A value for the TWT related parameter, as described in Web Intents. This value will be forwarded to all Web Intents calls.Example Values:twitterapi,twittermedia,twitter                   
 * @param {string}  lang   Language code for the rendered embed. This will affect the text and localization of the rendered HTML.Example Values: fr                   
 * @methodhttp GET
 */
exports.oembed = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/oembed.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'str', true);
	url = url_replace(url, args, 'url', 'str', true);
	url = url_replace(url, args, 'maxwidth', 'str');
	url = url_replace(url, args, 'hide_media', 'str');
	url = url_replace(url, args, 'hide_thread', 'str');
	url = url_replace(url, args, 'omit_script', 'str');
	url = url_replace(url, args, 'align', 'str');
	url = url_replace(url, args, 'related', 'str');
	url = url_replace(url, args, 'lang', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a collection of up to 100 user IDs belonging to users who have retweeted the tweet specified by the id parameter.This method offers similar data to GET statuses/retweets/:id and replaces API v1's GET statuses/:id/retweeted_by/ids method.                  
 * @category Tweets
 * @param {string} [format=json]  undefined
 * @param {string} [id]  The Tweet/status ID to return embed code for.Example Values: 99530515043983360                   
 * @param {string}  cursor   This param is semi-optional.Causes the list of IDs to be broken into pages of no more than 100 IDs at a time. The number of IDs returned is not guaranteed to be 100 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."The response from the API will include a previous_cursor and next_cursor to allow paging back and forth. See Using cursors to navigate collections for more information.While this method supports the cursor parameter, the entire result set can be returned in a single cursored collection. Using the count parameter with this method will not provide segmented cursors for use with this parameter.Example Values: 12893764510938                   
 * @param {string}  stringify_ids   Many programming environments will not consume our ids due to their size. Provide this option to have ids returned as strings instead. Read more about Twitter IDs, JSON and Snowflake.Example Values: true                   
 * @methodhttp GET
 */
exports.listRetweeters = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/retweeters/ids.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'str', true);
	url = url_replace(url, args, 'cursor', 'str');
	url = url_replace(url, args, 'stringify_ids', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns public statuses that match one or more filter predicates. Multiple parameters may be specified which allows most clients to use a single connection to the Streaming API. Both GET and POST requests are supported, but GET requests with too many parameters may cause the request to be rejected for excessive URL length. Use a POST request to avoid long URLs.The track, follow, and locations fields should be considered to be combined with an OR operator. track=foo and follow=1234 returns Tweets matching "foo" OR created by user 1234.The default access level allows up to 400 track keywords, 5,000 follow userids and 25 0.1-360 degree location boxes. If you need elevated access to the Streaming API, you should explore our partner providers of Twitter data here.                  
 * @category Streaming
 * @param {string} [format=json]  undefined
 * @param {string}  follow   *Note: At least one predicate parameter (follow, locations, or track) must be specified.A comma separated list of user IDs, indicating the users to return statuses for in the stream. See the follow parameter documentation for more information.https://dev.twitter.com/docs/streaming-apis/parameters#follow                   
 * @param {string}  track   *Note: At least one predicate parameter (follow, locations, or track) must be specified.Keywords to track. Phrases of keywords are specified by a comma-separated list. See the track parameter documentation for more information.https://dev.twitter.com/docs/streaming-apis/parameters#track                   
 * @param {string}  locations   *Note: At least one predicate parameter (follow, locations, or track) must be specified.Specifies a set of bounding boxes to track. See the locations parameter documentation for more information.https://dev.twitter.com/docs/streaming-apis/parameters#locations                   
 * @param {string}  delimited   Specifies whether messages should be length-delimited. See the delimited parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#delimited                   
 * @param {string}  stall_warnings   Specifies whether stall warnings should be delivered. See the stall_warnings parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#stall_warnings                   
 * @methodhttp POST
 */
exports.getStatusesWithFilter = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/filter.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'follow', 'str');
	url = url_replace(url, args, 'track', 'str');
	url = url_replace(url, args, 'locations', 'str');
	url = url_replace(url, args, 'delimited', 'str');
	url = url_replace(url, args, 'stall_warnings', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a small random sample of all public statuses. The Tweets returned by the default access level are the same, so if two different clients connect to this endpoint, they will see the same Tweets.                  
 * @category Streaming
 * @param {string} [format=json]  undefined
 * @param {string}  delimited   Specifies whether messages should be length-delimited. See the delimited parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#delimited                   
 * @param {string}  stall_warnings   Specifies whether stall warnings should be delivered. See the stall_warnings parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#stall_warnings                   
 * @methodhttp GET
 */
exports.getSample = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/sample.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'delimited', 'str');
	url = url_replace(url, args, 'stall_warnings', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * This endpoint requires special permission to access.Returns all public statuses. Few applications require this level of access. Creative use of a combination of other resources and various access levels can satisfy nearly every application use case.                  
 * @category Streaming
 * @param {string} [format=json]  undefined
 * @param {string}  count   The number of messages to backfill. See the count parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#count                   
 * @param {string}  delimited   Specifies whether messages should be length-delimited. See the delimited parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#delimited                   
 * @param {string}  stall_warnings   Specifies whether stall warnings should be delivered. See the stall_warnings parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#stall_warnings                   
 * @methodhttp GET
 */
exports.getAllPublicStatuses = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/statuses/firehose.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'count', 'str');
	url = url_replace(url, args, 'delimited', 'str');
	url = url_replace(url, args, 'stall_warnings', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Streams messages for a single user, as described in User streams.                  
 * @category Streaming
 * @param {string} [format=json]  undefined
 * @param {string}  delimited   Specifies whether messages should be length-delimited. See the delimited parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#delimited                   
 * @param {string}  stall_warnings   Specifies whether stall warnings should be delivered. See the stall_warnings parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#stall_warnings                   
 * @param {string}  with   Specifies whether to return information for just the authenticating user, or include messages from accounts the user follows. See the with parameter documentation the with parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#with                   
 * @param {string}  replies   Specifies whether to return additional @replies. See the replies parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#replies                   
 * @param {string}  track   Includes additional Tweets matching the specified keywords. Phrases of keywords are specified by a comma-separated list. See the track parameter documentation the track parameter documentation for more information.https://dev.twitter.com/docs/streaming-apis/parameters#track                   
 * @param {string}  locations   Includes additional Tweets falling within the specified bounding boxes. See the locations parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#locations                   
 * @param {string}  stringify_friend_ids   Specifies whether to send the friends list preamble as an array of integers or an array of strings. See the stringify_friend_ids parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#stringify_friend_ids                   
 * @methodhttp GET
 */
exports.streamsMessages = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/user.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'delimited', 'str');
	url = url_replace(url, args, 'stall_warnings', 'str');
	url = url_replace(url, args, 'with', 'str');
	url = url_replace(url, args, 'replies', 'str');
	url = url_replace(url, args, 'track', 'str');
	url = url_replace(url, args, 'locations', 'str');
	url = url_replace(url, args, 'stringify_friend_ids', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Streams messages for a single user, as described in User streams.                  
 * @category Streaming
 * @param {string} [format=json]  undefined
 * @param {string} [follow]  A comma separated list of user IDs, indicating the users to return statuses for in the stream. See the follow parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#follow                   
 * @param {string}  delimited   Specifies whether messages should be length-delimited. See the delimited parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#delimited                   
 * @param {string}  stall_warnings   Specifies whether stall warnings should be delivered. See the stall_warnings parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#stall_warnings                   
 * @param {string}  with   Specifies whether to return information for just the authenticating user, or include messages from accounts the user follows. See the with parameter documentation the with parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#with                   
 * @param {string}  replies   Specifies whether to return additional @replies. See the replies parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#replies                   
 * @param {string}  stringify_friend_ids   Specifies whether to send the friends list preamble as an array of integers or an array of strings. See the stringify_friend_ids parameter documentation for more information.                        https://dev.twitter.com/docs/streaming-apis/parameters#stringify_friend_ids                   
 * @methodhttp GET
 */
exports.getSite = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/site.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'follow', 'str', true);
	url = url_replace(url, args, 'delimited', 'str');
	url = url_replace(url, args, 'stall_warnings', 'str');
	url = url_replace(url, args, 'with', 'str');
	url = url_replace(url, args, 'replies', 'str');
	url = url_replace(url, args, 'stringify_friend_ids', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Report the specified user as a spam account to Twitter. Additionally performs the equivalent of POST blocks/create on behalf of the authenticated user.
 * @category Spam Reporting
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for. Either an id or screen_name is required for this method.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Either a id or screen_name is required for this method.
 * @methodhttp POST
 */
exports.reportUser = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/report_spam.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a variety of information about the user specified by the required user_id or screen_name parameter. The author's most recent Tweet will be returned inline when possible.                       GET users/lookup is used to retrieve a bulk collection of user objects.
 * @category User
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for. Either an id or screen_name is required for this method.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Either a id or screen_name is required for this method.
 * @param {boolean}  include_entities   The entities node will be disincluded when set to false.
 * @methodhttp GET
 */
exports.showUser = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/show.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'include_entities');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns fully-hydrated user objects for up to 100 users per request, as specified by comma-separated values passed to the user_id and/or screen_name parameters.                       This method is especially useful when used in conjunction with collections of user IDs returned from GET friends/ids and GET followers/ids.                       GET users/show is used to retrieve a single user object.
 * @category User
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   A comma separated list of screen names, up to 100 are allowed in a single request. You are strongly encouraged to use a POST for larger (up to 100 screen names) requests.
 * @param {integer}  user_id   A comma separated list of user IDs, up to 100 are allowed in a single request. You are strongly encouraged to use a POST for larger requests.
 * @param {integer}  include_entities   The entities node that may appear within embedded statuses will be disincluded when set to false.
 * @methodhttp GET
 */
exports.lookupUsers = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/lookup.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'include_entities', 'int');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Provides a simple, relevance-based search interface to public user accounts on Twitter. Try querying by topical interest, full name, company name, location, or other criteria. Exact match searches are not supported.                       Only the first 1,000 matching results are available.
 * @category User
 * @param {string} [format=json]  undefined
 * @param {string} [q]  The search query to run against people search.
 * @param {integer}  page   Specifies the page of results to retrieve.
 * @param {integer}  count   The number of potential user results to retrieve per page. This value has a maximum of 20.
 * @param {string}  include_entities   The entities node will be disincluded from embedded tweet objects when set to false.
 * @methodhttp GET
 */
exports.searchUsers = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/search.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'q', 'str', true);
	url = url_replace(url, args, 'page', 'int');
	url = url_replace(url, args, 'count', 'int');
	url = url_replace(url, args, 'include_entities', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a map of the available size variations of the specified user's profile banner. If the user has not uploaded a profile banner, a HTTP 404 will be served instead. This method can be used instead of string manipulation on the profile_banner_url returned in user objects as described in User Profile Images and Banners.The profile banner data available at each size variant's URL is in PNG format.
 * @category User
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.Example Values: 12345Note:: Specifies the ID of the user to befriend. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.Example Values: noradio
 * @methodhttp GET
 */
exports.getUserProfileBanner = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/profile_banner.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns Returns a collection of users that the specified user can "contribute" to.
 * @category User
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.Example Values: 12345Note:: Specifies the ID of the user to befriend. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.Example Values: noradio
 * @param {string}  include_entities   The entities node will be disincluded when set to false.Example Values: false
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp GET
 */
exports.getUserContributees = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/contributees.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'include_entities', 'str');
	url = url_replace(url, args, 'skip_status', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns Returns a collection of users who can contribute to the specified account.
 * @category User
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.Example Values: 12345Note:: Specifies the ID of the user to befriend. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.Example Values: noradio
 * @param {string}  include_entities   The entities node will be disincluded when set to false.                        Example Values: false
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp GET
 */
exports.getUserContributors = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/contributors.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'include_entities', 'str');
	url = url_replace(url, args, 'skip_status', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Access to Twitter's suggested user list. This returns the list of suggested user categories.
 * @category User
 * @param {string} [format=json]  undefined
 * @param {string}  lang   Restricts the suggested categories to the requested language. The language must be specified by the appropriate two letter ISO 639-1 representation. Currently supported languages are provided by the apis.twitter.languages() API request. Unsupported language codes will receive English (en) results. If you use lang in this request, ensure you also include it when requesting the GET users/suggestions/:slug list.
 * @methodhttp GET
 */
exports.usersSuggestions = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/suggestions.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'lang', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Access the users in a given category of the Twitter suggested user list.                       It is recommended that applications cache this data for no more than one hour.
 * @category User
 * @param {string} [format=json]  undefined
 * @param {string} [slug=twitter]  The short name of list or a category
 * @param {string}  lang   Restricts the suggested categories to the requested language. The language must be specified by the appropriate two letter ISO 639-1 representation. Currently supported languages are provided by the apis.twitter.languages() API request. Unsupported language codes will receive English (en) results. If you use lang in this request, ensure you also include it when requesting the GET users/suggestions/:slug list.
 * @methodhttp GET
 */
exports.usersSuggestionsCategory = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/suggestions/{slug}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'slug', 'str', true, "twitter");
	url = url_replace(url, args, 'lang', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Access the users in a given category of the Twitter suggested user list and return their most recent status if they are not a protected user.
 * @category User
 * @param {string} [format=json]  undefined
 * @param {string} [slug=twitter]  The short name of list or a category
 * @methodhttp GET
 */
exports.usersSuggestionsCategoryMembers = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/users/suggestions/{slug}/members.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'slug', 'str', true, "twitter");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the 20 most recent direct messages sent to the authenticating user. Includes detailed information about the sender and recipient user. You can request up to 200 direct messages per call, up to a maximum of 800 incoming DMs.
 * @category Direct Message
 * @param {string} [format=json]  undefined
 * @param {integer}  count   Specifies the number of direct messages to try and retrieve, up to a maximum of 200. The value of count is best thought of as a limit to the number of Tweets to return because suspended or deleted content is removed after the count has been applied.
 * @param {integer}  max_id   Returns results with an ID less than (that is, older than) or equal to the specified ID.
 * @param {integer}  page   Specifies the page of results to retrieve.
 * @param {integer}  since_id   Returns results with an ID greater than (that is, more recent than) the specified ID. There are limits to the number of Tweets which can be accessed through the API. If the limit of Tweets has occured since the since_id, the since_id will be forced to the oldest ID available.
 * @param {boolean}  include_entities   The entities node will not be included when set to false.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp GET
 */
exports.directMessages = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/direct_messages.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'count', 'int');
	url = url_replace(url, args, 'max_id', 'int');
	url = url_replace(url, args, 'page', 'int');
	url = url_replace(url, args, 'since_id', 'int');
	url = url_replace(url, args, 'include_entities');
	url = url_replace(url, args, 'skip_status', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the 20 most recent direct messages sent by the authenticating user. Includes detailed information about the sender and recipient user. You can request up to 200 direct messages per call, up to a maximum of 800 outgoing DMs.                       Important: This method requires an access token with RWD (read, write and direct message) permissions. Consult The Application Permission Model for more information.
 * @category Direct Message
 * @param {string} [format=json]  undefined
 * @param {integer}  count   Specifies the number of direct messages to try and retrieve, up to a maximum of 200. The value of count is best thought of as a limit to the number of Tweets to return because suspended or deleted content is removed after the count has been applied.
 * @param {integer}  max_id   Returns results with an ID less than (that is, older than) or equal to the specified ID.
 * @param {integer}  page   Specifies the page of results to retrieve.
 * @param {integer}  since_id   Returns results with an ID greater than (that is, more recent than) the specified ID. There are limits to the number of Tweets which can be accessed through the API. If the limit of Tweets has occured since the since_id, the since_id will be forced to the oldest ID available.
 * @param {boolean}  include_entities   The entities node will not be included when set to false.
 * @methodhttp GET
 */
exports.directMessagesSent = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/direct_messages/sent.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'count', 'int');
	url = url_replace(url, args, 'max_id', 'int');
	url = url_replace(url, args, 'page', 'int');
	url = url_replace(url, args, 'since_id', 'int');
	url = url_replace(url, args, 'include_entities');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a single direct message, specified by an id parameter. Like the /1.1/direct_messages.format request, this method will include the user objects of the sender and recipient.                       Important: This method requires an access token with RWD (read, write and direct message) permissions. Consult The Application Permission Model for more information.
 * @category Direct Message
 * @param {string} [format=json]  undefined
 * @param {integer}  id   The ID of the direct message.
 * @methodhttp GET
 */
exports.showDirectMessage = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/direct_messages/show.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'int');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Sends a new direct message to the specified user from the authenticating user. Requires both the user and text parameters and must be a POST. Returns the sent message in the requested format if successful.
 * @category Direct Message
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user who should receive the direct message. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user who should receive the direct message. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {string}  text   The text of your direct message. Be sure to URL encode as necessary, and keep the message under 140 characters.
 * @methodhttp POST
 */
exports.newDirectMessage = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/direct_messages/new.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'text', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Destroys the direct message specified in the required ID parameter. The authenticating user must be the recipient of the specified direct message.                       Important: This method requires an access token with RWD (read, write and direct message) permissions. Consult The Application Permission Model for more information.
 * @category Direct Message
 * @param {string} [format=json]  undefined
 * @param {string} [id]  The ID of the direct message to delete.
 * @param {boolean}  include_entities   The entities node will not be included when set to false.
 * @methodhttp DELETE
 */
exports.destroyDirectMessage = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/direct_messages/destroy.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'str', true);
	url = url_replace(url, args, 'include_entities');
	return http.delete({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Allows the authenticating users to follow the user specified in the ID parameter.                       Returns the befriended user in the requested format when successful. Returns a string describing the failure condition when unsuccessful. If you are already friends with the user a HTTP 403 may be returned, though for performance reasons you may get a 200 OK message even if the friendship already exists.                       Actions taken in this method are asynchronous and changes will be eventually consistent. Providing either screen_name or user_id is required.
 * @category Friendship
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the user for whom to befriend.
 * @param {integer}  user_id   The ID of the user for whom to befriend.
 * @param {boolean}  follow   Enable notifications for the target user.       
 * @methodhttp POST
 */
exports.follow = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friendships/create.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'follow');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * @category Friendship
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the user for whom to befriend.
 * @param {integer}  user_id   The ID of the user for whom to befriend.
 * @methodhttp DELETE
 */
exports.unfollow = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friendships/destroy.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	return http.delete({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns detailed information about the relationship between two arbitrary users. A choice has to be made between screen_name or id for both target and source parameters.
 * @category Friendship
 * @param {string} [format=json]  undefined
 * @param {integer}  source_id   The user_id of the subject user.
 * @param {string}  source_screen_name   The screen_name of the subject user.
 * @param {integer}  target_id   The user_id of the target user.
 * @param {string}  target_screen_name   The screen_name of the target user.
 * @methodhttp GET
 */
exports.showFriendship = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friendships/show.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'source_id', 'int');
	url = url_replace(url, args, 'source_screen_name', 'str');
	url = url_replace(url, args, 'target_id', 'int');
	url = url_replace(url, args, 'target_screen_name', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a collection of numeric IDs for every user who has a pending request to follow the authenticating user.
 * @category Friendship
 * @param {string} [format=json]  undefined
 * @param {integer}  cursor   Causes the list of connections to be broken into pages of no more than 5000 IDs at a time. The number of IDs returned is not guaranteed to be 5000 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                        The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.                   
 * @param {boolean}  stringify_ids   Many programming environments will not consume our Tweet ids due to their size. Provide this option to have ids returned as strings instead.
 * @methodhttp GET
 */
exports.incomingFriendships = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friendships/incoming.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'cursor', 'int');
	url = url_replace(url, args, 'stringify_ids');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a collection of numeric IDs for every protected user for whom the authenticating user has a pending follow request.
 * @category Friendship
 * @param {string} [format=json]  undefined
 * @param {integer}  cursor   Causes the list of connections to be broken into pages of no more than 5000 IDs at a time. The number of IDs returned is not guaranteed to be 5000 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                        The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.                   
 * @param {boolean}  stringify_ids   Many programming environments will not consume our Tweet ids due to their size. Provide this option to have ids returned as strings instead.
 * @methodhttp GET
 */
exports.outgoingFriendships = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friendships/outgoing.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'cursor', 'int');
	url = url_replace(url, args, 'stringify_ids');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Allows one to enable or disable retweets and device notifications from the specified user.
 * @category Friendship
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the user for whom to befriend.Example Values: noradio                   
 * @param {string}  user_id   The ID of the user for whom to befriend.Example Values: 12345                   
 * @param {string}  device   Enable/disable device notifications from the target user.Example Values: true, false                   
 * @param {string}  retweets   Enable/disable retweets from the target user.Example Values: true, false                   
 * @methodhttp POST
 */
exports.updateFriendships = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friendships/update.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'str');
	url = url_replace(url, args, 'device', 'str');
	url = url_replace(url, args, 'retweets', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the relationships of the authenticating user to the comma-separated list of up to 100 screen_names or user_ids provided. Values for connections can be: following, following_requested, followed_by, none.
 * @category Friendship
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the user for whom to befriend.Example Values: noradio                   
 * @param {string}  user_id   The ID of the user for whom to befriend.Example Values: 12345                   
 * @methodhttp GET
 */
exports.lookupFriendships = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friendships/lookup.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a collection of user_ids that the currently authenticated user does not want to receive retweets from.Use POST friendships/update to set the "no retweets" status for a given user account on behalf of the current user.
 * @category Friendship
 * @param {string} [format=json]  undefined
 * @param {string}  stringify_ids   Many programming environments will not consume our ids due to their size. Provide this option to have ids returned as strings instead. Read more about Twitter IDs, JSON and Snowflake. This parameter is especially important to use in Javascript environments.Example Values: true                   
 * @methodhttp GET
 */
exports.getUserIdRetweetNoWantedFrom = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friendships/no_retweets/ids.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'stringify_ids', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a cursored collection of user IDs for every user the specified user is following (otherwise known as their "friends").                       At this time, results are ordered with the most recent following first — however, this ordering is subject to unannounced change and eventual consistency issues. Results are given in groups of 5,000 user IDs and multiple "pages" of results can be navigated through using the next_cursor value in subsequent requests. See Using cursors to navigate collections for more information.                       This method is especially powerful when used in conjunction with GET users/lookup, a method that allows you to convert user IDs into full user objects in bulk. Either a screen_name or a user_id must be provided.
 * @category Friends and Followers
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the user for whom to befriend.
 * @param {integer}  user_id   The ID of the user for whom to befriend.
 * @param {integer}  cursor   Causes the list of connections to be broken into pages of no more than 5000 IDs at a time. The number of IDs returned is not guaranteed to be 5000 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                        The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.                   
 * @param {boolean}  stringify_ids   Many programming environments will not consume our Tweet ids due to their size. Provide this option to have ids returned as strings instead.
 * @methodhttp GET
 */
exports.friends = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friends/ids.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'cursor', 'int');
	url = url_replace(url, args, 'stringify_ids');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a cursored collection of user IDs for every user following the specified user.                       At this time, results are ordered with the most recent following first — however, this ordering is subject to unannounced change and eventual consistency issues. Results are given in groups of 5,000 user IDs and multiple "pages" of results can be navigated through using the next_cursor value in subsequent requests. See Using cursors to navigate collections for more information.                       This method is especially powerful when used in conjunction with GET users/lookup, a method that allows you to convert user IDs into full user objects in bulk.
 * @category Friends and Followers
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the user for whom to befriend.
 * @param {integer}  user_id   The ID of the user for whom to befriend.
 * @param {integer}  cursor   Causes the list of connections to be broken into pages of no more than 5000 IDs at a time. The number of IDs returned is not guaranteed to be 5000 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                        The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.                   
 * @param {boolean}  stringify_ids   Many programming environments will not consume our Tweet ids due to their size. Provide this option to have ids returned as strings instead.
 * @methodhttp GET
 */
exports.followers = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/followers/ids.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'cursor', 'int');
	url = url_replace(url, args, 'stringify_ids');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a cursored collection of user objects for users following the specified userAt this time, results are ordered with the most recent following first  however, this ordering is subject to unannounced change and eventual consistency issues. Results are given in groups of 20 users and multiple "pages" of results can be navigated through using the next_cursor value in subsequent requests. See Using cursors to navigate collections for more information.
 * @category Friends and Followers
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the user for whom to befriend.
 * @param {integer}  user_id   The ID of the user for whom to befriend.
 * @param {integer}  cursor   Causes the list of connections to be broken into pages of no more than 5000 IDs at a time. The number of IDs returned is not guaranteed to be 5000 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                        The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.                   
 * @param {boolean}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.Example Values: false
 * @param {}  include_user_entities   WThe user object entities node will be disincluded when set to false.Example Values: false
 * @methodhttp GET
 */
exports.followersList = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/followers/list.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'cursor', 'int');
	url = url_replace(url, args, 'skip_status');
	url = url_replace(url, args, 'include_user_entities');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a cursored collection of user objects for every user the specified user is following (otherwise known as their "friends").At this time, results are ordered with the most recent following first — however, this ordering is subject to unannounced change and eventual consistency issues. Results are given in groups of 20 users and multiple "pages" of results can be navigated through using the next_cursor value in subsequent requests. See Using cursors to navigate collections for more information.
 * @category Friends and Followers
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the user for whom to befriend.
 * @param {integer}  user_id   The ID of the user for whom to befriend.
 * @param {integer}  cursor   Causes the list of connections to be broken into pages of no more than 5000 IDs at a time. The number of IDs returned is not guaranteed to be 5000 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                        The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.                   
 * @param {boolean}  include_user_entities   When set to either true, t or 1 statuses will not be included in the returned user objects.Example Values: false
 * @param {boolean}  skip_status   The user object entities node will be disincluded when set to false.Example Values: false
 * @methodhttp GET
 */
exports.friendsList = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friends/list.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'cursor', 'int');
	url = url_replace(url, args, 'include_user_entities');
	url = url_replace(url, args, 'skip_status');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns an HTTP 200 OK response code and a representation of the requesting user if authentication was successful; returns a 401 status code and an error message if not. Use this method to test if supplied user credentials are valid.
 * @category Account
 * @param {string} [format=json]  undefined
 * @param {boolean}  include_entities   The entities node will not be included when set to false.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp GET
 */
exports.accountVerifyCredentials = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/verify_credentials.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'include_entities');
	url = url_replace(url, args, 'skip_status', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Sets one or more hex values that control the color scheme of the authenticating user's profile page on twitter.com. Each parameter's value must be a valid hexidecimal value, and may be either three or six characters (ex: #fff or #ffffff).
 * @category Account
 * @param {string} [format=json]  undefined
 * @param {string}  profile_background_color   Profile background color.
 * @param {string}  profile_link_color   Profile link color.
 * @param {string}  profile_sidebar_border_color   Profile sidebar's border color.
 * @param {string}  profile_sidebar_fill_color   Profile sidebar's background color.
 * @param {string}  profile_text_color   Profile text color.
 * @param {boolean}  include_entities   The entities node will not be included when set to false.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp POST
 */
exports.accountUpdateProfileColors = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/update_profile_colors.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'profile_background_color', 'str');
	url = url_replace(url, args, 'profile_link_color', 'str');
	url = url_replace(url, args, 'profile_sidebar_border_color', 'str');
	url = url_replace(url, args, 'profile_sidebar_fill_color', 'str');
	url = url_replace(url, args, 'profile_text_color', 'str');
	url = url_replace(url, args, 'include_entities');
	url = url_replace(url, args, 'skip_status', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Updates the authenticating user's profile image. Note that this method expects raw multipart data, not a URL to an image.                       This method asynchronously processes the uploaded file before updating the user's profile image URL. You can either update your local cache the next time you request the user's information, or, at least 5 seconds after uploading the image, ask for the updated URL using GET users/show.
 * @category Account
 * @param {string} [format=json]  undefined
 * @param {string} [image]  The avatar image for the profile, base64-encoded. Must be a valid GIF, JPG, or PNG image of less than 700 kilobytes in size. Images with width larger than 500 pixels will be scaled down. Animated GIFs will be converted to a static GIF of the first frame, removing the animation.
 * @param {boolean}  include_entities   The entities node will not be included when set to false.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp POST
 */
exports.accountUpdateProfileImage = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/update_profile_image.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'image', 'str', true);
	url = url_replace(url, args, 'include_entities');
	url = url_replace(url, args, 'skip_status', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Updates the authenticating user's profile background image. This method can also be used to enable or disable the profile background image.                       Although each parameter is marked as optional, at least one of image, tile or use must be provided when making this request.
 * @category Account
 * @param {string} [format=json]  undefined
 * @param {string}  tile   Whether or not to tile the background image. If set to true, t or 1 the background image will be displayed tiled. The image will not be tiled otherwise.
 * @param {string}  image   The background image for the profile, base64-encoded. Must be a valid GIF, JPG, or PNG image of less than 800 kilobytes in size. Images with width larger than 2048 pixels will be forcibly scaled down. The image must be provided as raw multipart data, not a URL.
 * @param {boolean}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @param {string}  include_entities   The entities node will not be included when set to false.
 * @param {string}  use   Determines whether to display the profile background image or not. When set to true, t or 1 the background image will be displayed if an image is being uploaded with the request, or has been uploaded previously. An error will be returned if you try to use a background image when one is not being uploaded or does not exist. If this parameter is defined but set to anything other than true, t or 1, the background image will stop being used.
 * @methodhttp POST
 */
exports.accountUpdateProfileBackgroundImage = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/update_profile_background_image.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'tile', 'str');
	url = url_replace(url, args, 'image', 'str');
	url = url_replace(url, args, 'skip_status');
	url = url_replace(url, args, 'include_entities', 'str');
	url = url_replace(url, args, 'use', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Sets values that users are able to set under the "Account" tab of their settings page. Only the parameters specified will be updated.
 * @category Account
 * @param {string} [format=json]  undefined
 * @param {string}  name   Full name associated with the profile. Maximum of 20 characters.
 * @param {string}  url   URL associated with the profile. Will be prepended with "http://" if not present. Maximum of 100 characters.
 * @param {string}  location   The city or country describing where the user of the account is located. The contents are not normalized or geocoded in any way. Maximum of 30 characters.
 * @param {string}  description   A description of the user owning the account. Maximum of 160 characters.
 * @param {boolean}  include_entities   The entities node will not be included when set to false.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp POST
 */
exports.accountUpdateProfile = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/update_profile.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'name', 'str');
	url = url_replace(url, args, 'url', 'str');
	url = url_replace(url, args, 'location', 'str');
	url = url_replace(url, args, 'description', 'str');
	url = url_replace(url, args, 'include_entities');
	url = url_replace(url, args, 'skip_status', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Sets which device Twitter delivers updates to for the authenticating user. Sending none as the device parameter will disable SMS updates.
 * @category Account
 * @param {string} [format=json]  undefined
 * @param {string} [device]  Must be one of: sms, none.
 * @param {boolean}  include_entities   When set to either true, t or 1, each tweet will include a node called "entities,". This node offers a variety of metadata about the tweet in a discreet structure, including: user_mentions, urls, and hashtags. While entities are opt-in on timelines at present, they will be made a default component of output in the future.
 * @methodhttp POST
 */
exports.accountUpdateDeliveryDevice = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/update_delivery_device.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'device', 'str', true);
	url = url_replace(url, args, 'include_entities');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Updates the authenticating user's settings.
 * @category Account
 * @param {string} [format=json]  undefined
 * @param {string}  trend_location_woeid   The Yahoo! Where On Earth ID to use as the user's default trend location. Global information is available by using 1 as the WOEID. The woeid must be one of the locations returned by GET trends/available.Example Values: 1
 * @param {string}  sleep_time_enabled   When set to true, t or 1, will enable sleep time for the user. Sleep time is the time when push or SMS notifications should not be sent to the user.Example Values: true
 * @param {string}  start_sleep_time   The hour that sleep time should begin if it is enabled. The value for this parameter should be provided in ISO8601 format (i.e. 00-23). The time is considered to be in the same timezone as the user's time_zone setting.Example Values: 13
 * @param {string}  end_sleep_time   The hour that sleep time should end if it is enabled. The value for this parameter should be provided in ISO8601 format (i.e. 00-23). The time is considered to be in the same timezone as the user's time_zone setting.Example Values: 13
 * @param {string}  time_zone   The timezone dates and times should be displayed in for the user. The timezone must be one of the Rails TimeZone names.Example Values: Europe/Copenhagen, Pacific/Tongatapu
 * @param {string}  sleep_time_enabled   The language which Twitter should render in for this user. The language must be specified by the appropriate two letter ISO 639-1 representation. Currently supported languages are provided by GET help/languages.Example Values: it, en, es
 * @methodhttp POST
 */
exports.updateAccountSettings = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/settings.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'trend_location_woeid', 'str');
	url = url_replace(url, args, 'sleep_time_enabled', 'str');
	url = url_replace(url, args, 'start_sleep_time', 'str');
	url = url_replace(url, args, 'end_sleep_time', 'str');
	url = url_replace(url, args, 'time_zone', 'str');
	url = url_replace(url, args, 'sleep_time_enabled', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns settings (including current trend, geo and sleep time information) for the authenticating user.
 * @category Account
 * @param {string} [format=json]  undefined
 * @methodhttp GET
 */
exports.getAccountSettings = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/settings.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Removes the uploaded profile banner for the authenticating user. Returns HTTP 200 upon success.
 * @category Account
 * @param {string} [format=json]  undefined
 * @methodhttp GET
 */
exports.removeProfileBanner = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/remove_profile_banner.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Uploads a profile banner on behalf of the authenticating user. For best results, upload an inferior 5MB image that is exactly 1252px by 626px. Images will be resized for a number of display options. Users with an uploaded profile banner will have a profile_banner_url node in their Users objects. More information about sizing variations can be found in User Profile Images and Banners and GET users/profile_banner.Profile banner images are processed asynchronously. The profile_banner_url and its variant sizes will not necessary be available directly after upload.
 * @category Account
 * @param {string} [format=json]  undefined
 * @param {string} [banner]  The Base64-encoded or raw image data being uploaded as the user's new profile banner.
 * @param {string}  width   The width of the preferred section of the image being uploaded in pixels. Use with height, offset_left, and offset_top to select the desired region of the image to use.                Note: If providing any one of the height, width, offset_left, or offset_top parameters, you must provide all of the sizing parameters.
 * @param {string}  height   The height of the preferred section of the image being uploaded in pixels. Use with width, offset_left, and offset_top to select the desired region of the image to use.                Note: If providing any one of the height, width, offset_left, or offset_top parameters, you must provide all of the sizing parameters.
 * @param {string}  offset_left   The number of pixels by which to offset the uploaded image from the left. Use with height, width, and offset_top to select the desired region of the image to use.                Note: If providing any one of the height, width, offset_left, or offset_top parameters, you must provide all of the sizing parameters.
 * @param {string}  offset_top   The number of pixels by which to offset the uploaded image from the top. Use with height, width, and offset_left to select the desired region of the image to use.                Note: If providing any one of the height, width, offset_left, or offset_top parameters, you must provide all of the sizing parameters.
 * @methodhttp GET
 */
exports.updateProfileBanner = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/account/update_profile_banner.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'banner', 'str', true);
	url = url_replace(url, args, 'width', 'str');
	url = url_replace(url, args, 'height', 'str');
	url = url_replace(url, args, 'offset_left', 'str');
	url = url_replace(url, args, 'offset_top', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns all lists the authenticating or specified user subscribes to, including their own. The user is specified using the user_id or screen_name parameters. If no user is given, the authenticating user is used.                       This method used to be GET lists in version 1.0 of the API and has been renamed for consistency with other call.
 * @category List
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {string}  reverse   Set this to true if you would like owned lists to be returned first. See description above for information on how this parameter works.Example Values: true
 * @methodhttp GET
 */
exports.getLists = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/list.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'reverse', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the lists the specified user has been added to. If user_id or screen_name are not provided the memberships for the authenticating user are returned.
 * @category List
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {string}  filter_to_owned_lists   When set to true, t or 1, will return just lists the authenticating user owns, and the user represented by user_id or screen_name is a member of.       
 * @param {integer}  cursor   Breaks the results into pages. A single page contains 20 lists. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
 * @methodhttp GET
 */
exports.listsMemberships = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/memberships.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'filter_to_owned_lists', 'str');
	url = url_replace(url, args, 'cursor', 'int');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Obtain a collection of the lists the specified user is subscribed to, 20 lists per page by default. Does not include the user's own lists.
 * @category List
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {integer}  cursor   Breaks the results into pages. A single page contains 20 lists. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list. It is recommended to always use cursors when the method supports them.
 * @methodhttp GET
 */
exports.userListsSubscriptions = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/subscriptions.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'cursor', 'int');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the lists owned by the specified Twitter user. Private lists will only be shown if the authenticated user is also the owner of the lists.
 * @category List
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.                    A user_id or screen_name must be provided.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.                    A user_id or screen_name must be provided.
 * @param {string}  count   The amount of results to return per page. Defaults to 20. No more than 1000 results will ever be returned in a single page.
 * @param {integer}  cursor   Breaks the results into pages. A single page contains 20 lists. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list. It is recommended to always use cursors when the method supports them.
 * @methodhttp GET
 */
exports.userListsOwnerships = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/ownerships.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'count', 'str');
	url = url_replace(url, args, 'cursor', 'int');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the specified list. Private lists will only be shown if the authenticated user owns the specified list. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List
 * @param {string} [format=json]  undefined
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {integer}  list_id   The numerical id of the list.
 * @methodhttp GET
 */
exports.userListShow = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/show.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'owner_id', 'int');
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'list_id', 'int');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns tweet timeline for members of the specified list. Retweets are included by default. You can use the include_rts=false parameter to omit retweet objects.
 * @category List
 * @param {string} [format=json]  undefined
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {string}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  count   Specifies the number of results to retrieve per "page."
 * @param {integer}  max_id   Returns results with an ID less than (that is, older than) or equal to the specified ID.
 * @param {string}  since_id   Returns results with an ID greater than (that is, more recent than) the specified ID. There are limits to the number of Tweets which can be accessed through the API. If the limit of Tweets has occured since the since_id, the since_id will be forced to the oldest ID available.
 * @param {boolean}  include_entities   Entities are ON by default in API 1.1, each tweet includes a node called "entities". This node offers a variety of metadata about the tweet in a discreet structure, including: user_mentions, urls, and hashtags. You can omit entities from the result by using include_entities=false
 * @param {string}  include_rts   When set to either true, t or 1, the list timeline will contain native retweets (if they exist) in addition to the standard stream of tweets. The output format of retweeted tweets is identical to the representation you see in home_timeline.
 * @methodhttp GET
 */
exports.listStatuses = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/statuses.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'owner_id', 'str');
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'count', 'int');
	url = url_replace(url, args, 'max_id', 'int');
	url = url_replace(url, args, 'since_id', 'str');
	url = url_replace(url, args, 'include_entities');
	url = url_replace(url, args, 'include_rts', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Creates a new list for the authenticated user. Note that you can't create more than 20 lists per account.
 * @category List
 * @param {string} [format=json]  undefined
 * @param {string} [name]  The name for the list.                        A list's name must start with a letter and can consist only of 25 or fewer letters, numbers, "-", or "_" characters.                   
 * @param {string}  mode   Whether your list is public or private. Values can be public or private. If no mode is specified the list will be public.
 * @param {string}  description   The description to give the list.
 * @methodhttp POST
 */
exports.createList = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/create.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'name', 'str', true);
	url = url_replace(url, args, 'mode', 'str');
	url = url_replace(url, args, 'description', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Updates the specified list. The authenticated user must own the list to be able to update it. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List
 * @param {string} [format=json]  undefined
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {string}  name   The name for the list.
 * @param {string}  mode   Whether your list is public or private. Values can be public or private. If no mode is specified the list will be public.
 * @param {string}  description   The description to give the list.
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {string}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @methodhttp POST
 */
exports.updateList = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/update.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'name', 'str');
	url = url_replace(url, args, 'mode', 'str');
	url = url_replace(url, args, 'description', 'str');
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Deletes the specified list. The authenticated user must own the list to be able to destroy it. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List
 * @param {string} [format=json]  undefined
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {string}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @methodhttp POST
 */
exports.destroyList = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/destroy.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'str');
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the members of the specified list. Private list members will only be shown if the authenticated user owns the specified list. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List Members
 * @param {string} [format=json]  undefined
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @param {integer}  list_id   The user ID of the user who owns the list being requested by a slug.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {integer}  cursor   Causes the collection of list members to be broken into "pages" of somewhat consistent size. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                        The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.                   
 * @param {boolean}  include_entities   The entities node will be disincluded when set to false.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp GET
 */
exports.listmembers = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/members.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'int');
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'cursor', 'int');
	url = url_replace(url, args, 'include_entities');
	url = url_replace(url, args, 'skip_status', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Check if the specified user is a member of the specified list.
 * @category List Members
 * @param {string} [format=json]  undefined
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  include_entities   When set to either true, t or 1, each tweet will include a node called "entities". This node offers a variety of metadata about the tweet in a discreet structure, including: user_mentions, urls, and hashtags. While entities are opt-in on timelines at present, they will be made a default component of output in the future.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp GET
 */
exports.memberIsInList = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/members/show.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'include_entities', 'str');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'skip_status', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Add a member to a list. The authenticated user must own the list to be able to add members to it. Note that lists can't have more than 500 members. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List Members
 * @param {string} [format=json]  undefined
 * @param {integer} [list_id]  The numerical id of the list.
 * @param {string} [slug]  You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @methodhttp POST
 */
exports.listAddMember = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/members/create.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'list_id', 'int', true);
	url = url_replace(url, args, 'slug', 'str', true);
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'int');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Adds multiple members to a list, by specifying a comma-separated list of member ids or screen names. The authenticated user must own the list to be able to add members to it. Note that lists can't have more than 500 members, and you are limited to adding up to 100 members to a list at a time with this method.                       Please note that there can be issues with lists that rapidly remove and add memberships. Take care when using these methods such that you are not too rapidly switching between removals and adds on the same list. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List Members
 * @param {string} [format=json]  undefined
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {string}  user_id   A comma separated list of user IDs, up to 100 are allowed in a single request.
 * @param {string}  screen_name   A comma separated list of screen names, up to 100 are allowed in a single request.
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @methodhttp POST
 */
exports.listAddMembers = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/members/create_all.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'user_id', 'str');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'int');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Removes the specified member from the list. The authenticated user must be the list's owner to remove members from the list. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List Members
 * @param {string} [format=json]  undefined
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {string}  user_id   The ID of the user to remove from the list. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to remove from the list. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @methodhttp POST
 */
exports.listRemoveMember = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/members/destroy.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'user_id', 'str');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'int');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Removes multiple members from a list, by specifying a comma-separated list of member ids or screen names. The authenticated user must own the list to be able to remove members from it. Note that lists can't have more than 500 members, and you are limited to removing up to 100 members to a list at a time with this method.Please note that there can be issues with lists that rapidly remove and add memberships. Take care when using these methods such that you are not too rapidly switching between removals and adds on the same list.
 * @category List Members
 * @param {string} [format=json]  undefined
 * @param {integer}  list_id   The numerical id of the list.                    Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.                    Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @param {string}  user_id   The ID of the user to remove from the list. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to remove from the list. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @methodhttp POST
 */
exports.listRemoveAllMember = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/members/destroy_all.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'user_id', 'str');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'int');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the subscribers of the specified list. Private list subscribers will only be shown if the authenticated user owns the specified list. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List Subscribers
 * @param {string} [format=json]  undefined
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @param {integer}  cursor   Breaks the results into pages. A single page contains 20 lists. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
 * @param {string}  include_entities   When set to either true, t or 1, each tweet will include a node called "entities". This node offers a variety of metadata about the tweet in a discreet structure, including: user_mentions, urls, and hashtags. While entities are opt-in on timelines at present, they will be made a default component of output in the future.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp GET
 */
exports.listSubscribers = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/subscribers.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'int');
	url = url_replace(url, args, 'cursor', 'int');
	url = url_replace(url, args, 'include_entities', 'str');
	url = url_replace(url, args, 'skip_status', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Check if the specified user is a subscriber of the specified list. Returns the user if they are subscriber. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List Subscribers
 * @param {string} [format=json]  undefined
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @param {integer}  user_id   The ID of the user for whom to return results for. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @param {string}  screen_name   The screen name of the user for whom to return results for. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {string}  include_entities   When set to either true, t or 1, each tweet will include a node called "entities". This node offers a variety of metadata about the tweet in a discreet structure, including: user_mentions, urls, and hashtags. While entities are opt-in on timelines at present, they will be made a default component of output in the future.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp GET
 */
exports.isSubscriber = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/subscribers/show.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'int');
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'include_entities', 'str');
	url = url_replace(url, args, 'skip_status', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Subscribes the authenticated user to the specified list. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List Subscribers
 * @param {string} [format=json]  undefined
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @methodhttp POST
 */
exports.listSubscribeMe = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/subscribers/create.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'int');
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Unsubscribes the authenticated user from the specified list. Either a list_id or a slug is required. If providing a list_slug, an owner_screen_name or owner_id is also required.
 * @category List Subscribers
 * @param {string} [format=json]  undefined
 * @param {string}  owner_screen_name   The screen name of the user who owns the list being requested by a slug.
 * @param {integer}  owner_id   The user ID of the user who owns the list being requested by a slug.
 * @param {integer}  list_id   The numerical id of the list.
 * @param {string}  slug   You can identify a list by its slug instead of its numerical id. If you decide to do so, note that you'll also have to specify the list owner using the owner_id or owner_screen_name parameters.
 * @methodhttp POST
 */
exports.listUnsubscribeMe = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/lists/subscribers/destroy.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'owner_screen_name', 'str');
	url = url_replace(url, args, 'owner_id', 'int');
	url = url_replace(url, args, 'list_id', 'int');
	url = url_replace(url, args, 'slug', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the 20 most recent Tweets favorited by the authenticating or specified user. If you do not provide either a user_id or screen_name to this method, it will assume you are requesting on behalf of the authenticating user. Specify one or the other for best results.
 * @category Favorite
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for.
 * @param {string}  screen_name   The screen name of the user for whom to return results for.
 * @param {integer}  count   Specifies the number of records to retrieve. Must be less than or equal to 200. Defaults to 20.
 * @param {integer}  since_id   Returns results with an ID greater than (that is, more recent than) the specified ID. There are limits to the number of Tweets which can be accessed through the API. If the limit of Tweets has occured since the since_id, the since_id will be forced to the oldest ID available.
 * @param {integer}  max_id   Returns results with an ID less than (that is, older than) or equal to the specified ID.
 * @param {integer}  include_entities   The entities node will be omitted when set to false.
 * @param {string}  page   undefined
 * @methodhttp GET
 */
exports.favorites = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/favorites/list.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'count', 'int');
	url = url_replace(url, args, 'since_id', 'int');
	url = url_replace(url, args, 'max_id', 'int');
	url = url_replace(url, args, 'include_entities', 'int');
	url = url_replace(url, args, 'page', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Favorites the status specified in the ID parameter as the authenticating user. Returns the favorite status when successful.                   This process invoked by this method is asynchronous. The immediately returned status may not indicate the resultant favorited status of the tweet. A 200 OK response from this method will indicate whether the intended action was successful or not.
 * @category Favorite
 * @param {string} [format=json]  undefined
 * @param {integer} [id]  The numerical ID of the desired status.
 * @param {boolean}  include_entities   The entities node will be omitted when set to false.
 * @methodhttp POST
 */
exports.favoriteStatus = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/favorites/create/{id}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'int', true);
	url = url_replace(url, args, 'include_entities');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Un-favorites the status specified in the ID parameter as the authenticating user. Returns the un-favorited status in the requested format when successful.                   This process invoked by this method is asynchronous. The immediately returned status may not indicate the resultant favorited status of the tweet. A 200 OK response from this method will indicate whether the intended action was successful or not.
 * @category Favorite
 * @param {string} [format=json]  undefined
 * @param {integer} [id]  The numerical ID of the desired status.
 * @param {boolean}  include_entities   The entities node will be omitted when set to false.
 * @methodhttp DELETE
 */
exports.unfavoriteStatus = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/favorites/destroy/{id}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'int', true);
	url = url_replace(url, args, 'include_entities');
	return http.delete({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Blocks the specified user from following the authenticating user. In addition the blocked user will not show in the authenticating users mentions or timeline (unless retweeted by another user). If a follow or friend relationship exists it is destroyed.
 * @category Block
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the potentially blocked user. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {integer}  user_id   The screen name of the potentially blocked user. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {integer}  include_entities   The entities node will not be included when set to false.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp POST
 */
exports.blockUser = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/blocks/create.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'include_entities', 'int');
	url = url_replace(url, args, 'skip_status', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Un-blocks the user specified in the ID parameter for the authenticating user. Returns the un-blocked user in the requested format when successful. If relationships existed before the block was instated, they will not be restored.
 * @category Block
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The screen name of the potentially blocked user. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {integer}  user_id   The screen name of the potentially blocked user. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {integer}  include_entities   The entities node will not be included when set to false.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @methodhttp DELETE
 */
exports.unblockUser = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/blocks/destroy.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'include_entities', 'int');
	url = url_replace(url, args, 'skip_status', 'str');
	return http.delete({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a collection of user objects that the authenticating user is blocking.                   Important On October 15, 2012 this method will become cursored by default, altering the default response format. See Using cursors to navigate collections for more details on how cursoring works.
 * @category Block
 * @param {string} [format=json]  undefined
 * @param {integer}  include_entities   The entities node will not be included when set to false.
 * @param {string}  skip_status   When set to either true, t or 1 statuses will not be included in the returned user objects.
 * @param {integer}  cursor   Causes the list of blocked users to be broken into pages of no more than 5000 IDs at a time. The number of IDs returned is not guaranteed to be 5000 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                    The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.               
 * @methodhttp GET
 */
exports.blocksList = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/blocks/list.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'include_entities', 'int');
	url = url_replace(url, args, 'skip_status', 'str');
	url = url_replace(url, args, 'cursor', 'int');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns an array of numeric user ids the authenticating user is blocking.                   Important On October 15, 2012 this method will become cursored by default, altering the default response format. See Using cursors to navigate collections for more details on how cursoring works.
 * @category Block
 * @param {string} [format=json]  undefined
 * @param {boolean}  sringify_ids   Many programming environments will not consume our ids due to their size. Provide this option to have ids returned as strings instead.
 * @param {integer}  cursor   Causes the list of blocked users to be broken into pages of no more than 5000 IDs at a time. The number of IDs returned is not guaranteed to be 5000 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                    The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.               
 * @methodhttp GET
 */
exports.blockedIds = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/blocks/ids.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'sringify_ids');
	url = url_replace(url, args, 'cursor', 'int');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * The user specified in the id is blocked by the authenticated user and reported as a spammer.
 * @category Spam Reporting
 * @param {string} [format=json]  undefined
 * @param {string}  screen_name   The ID or screen_name of the user you want to report as a spammer. Helpful for disambiguating when a valid screen name is also a user ID.
 * @param {integer}  user_id   The ID of the user you want to report as a spammer. Helpful for disambiguating when a valid user ID is also a valid screen name.
 * @methodhttp POST
 */
exports.reportSpam = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/report_spam.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'user_id', 'int');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the authenticated user's saved search queries.
 * @category Saved Search
 * @param {string} [format=json]  undefined
 * @methodhttp GET
 */
exports.savedSearches = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/saved_searches/list.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Retrieve the information for the saved search represented by the given id. The authenticating user must be the owner of saved search ID being requested.
 * @category Saved Search
 * @param {string} [format=json]  undefined
 * @param {string} [id]  Retrieve the information for the saved search represented by the given id. The authenticating user must be the owner of saved search ID being requested.
 * @methodhttp GET
 */
exports.savedSearchesById = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/saved_searches/show/{id}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'str', true);
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Create a new saved search for the authenticated user. A user may only have 25 saved searches.
 * @category Saved Search
 * @param {string} [format=json]  undefined
 * @param {string} [query]  The query of the search the user would like to save.
 * @methodhttp POST
 */
exports.createSavedSearch = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/saved_searches/create.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'query', 'str', true);
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Destroys a saved search for the authenticating user. The authenticating user must be the owner of saved search id being destroyed.
 * @category Saved Search
 * @param {string} [format=json]  undefined
 * @param {string} [id]  The ID of the saved search.
 * @methodhttp DELETE
 */
exports.deleteSavedSearch = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/saved_searches/destroy/{id}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'str', true);
	return http.delete({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the locations that Twitter has trending topic information for.                   The response is an array of "locations" that encode the location's WOEID and some other human-readable information such as a canonical name and country the location belongs in.                   A WOEID is a Yahoo! Where On Earth ID.
 * @category Local Trends
 * @param {string} [format=json]  undefined
 * @methodhttp GET
 */
exports.userTrendingLocations = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/trends/available.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the top 10 trending topics for a specific WOEID, if trending information is available for it.                   The response is an array of "trend" objects that encode the name of the trending topic, the query parameter that can be used to search for the topic on Twitter Search, and the Twitter Search URL.                   This information is cached for 5 minutes. Requesting more frequently than that will not return any more data, and will count against your rate limit usage.
 * @category Local Trends
 * @param {string} [format=json]  undefined
 * @param {integer} [id]  The Yahoo! Where On Earth ID of the location to return trending information for. Global information is available by using 1 as the WOEID.
 * @param {string} [exclude]  Setting this equal to hashtags will remove all hashtags from the trends list.
 * @methodhttp GET
 */
exports.trendingTopics = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/trends/place.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'id', 'int', true);
	url = url_replace(url, args, 'exclude', 'str', true);
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the locations that Twitter has trending topic information for, closest to a specified location.                   The response is an array of "locations" that encode the location's WOEID and some other human-readable information such as a canonical name and country the location belongs in.                   A WOEID is a Yahoo! Where On Earth ID.
 * @category Local Trends
 * @param {string} [format=json]  undefined
 * @param {integer}  lat   If provided with a long parameter the available trend locations will be sorted by distance, nearest to furthest, to the co-ordinate pair. The valid ranges for longitude is -180.0 to +180.0 (West is negative, East is positive) inclusive.
 * @param {integer} [long]  If provided with a lat parameter the available trend locations will be sorted by distance, nearest to furthest, to the co-ordinate pair. The valid ranges for longitude is -180.0 to +180.0 (West is negative, East is positive) inclusive.
 * @methodhttp GET
 */
exports.closestTrendsTo = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/trends/closest.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'lat', 'int');
	url = url_replace(url, args, 'long', 'int', true);
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Search for places that can be attached to a statuses/update. Given a latitude and a longitude pair, an IP address, or a name, this request will return a list of all the valid places that can be used as the place_id when updating a status.                   Conceptually, a query can be made from the user's location, retrieve a list of places, have the user validate the location he or she is at, and then send the ID of this location with a call to POST statuses/update.                   This is the recommended method to use find places that can be attached to statuses/update. Unlike GET geo/reverse_geocode which provides raw data access, this endpoint can potentially re-order places with regards to the user who is authenticated. This approach is also preferred for interactive place matching with the user.
 * @category Geo
 * @param {string} [format=json]  undefined
 * @param {string}  ip   An IP address. Used when attempting to fix geolocation based off of the user's IP address.
 * @param {integer}  long   The longitude to search around. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This parameter will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding lat parameter.
 * @param {integer}  max_results   A hint as to the number of results to return. This does not guarantee that the number of results returned will equal max_results, but instead informs how many "nearby" results to return. Ideally, only pass in the number of places you intend to display to the user here.
 * @param {string}  granularity   This is the minimal granularity of place types to return and must be one of: poi, neighborhood, city, admin or country. If no granularity is provided for the request neighborhood is assumed.                    Setting this to city, for example, will find places which have a type of city, admin or country.               
 * @param {integer}  lat   The latitude to search around. This parameter will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding long parameter.
 * @param {string}  accuracy   A hint on the "region" in which to search. If a number, then this is a radius in meters, but it can also take a string that is suffixed with ft to specify feet. If this is not passed in, then it is assumed to be 0m. If coming from a device, in practice, this value is whatever accuracy the device has measuring its location (whether it be coming from a GPS, WiFi triangulation, etc.).
 * @param {string}  contained_within   This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.                    Specify a place_id. For example, to scope all results to places within "San Francisco, CA USA", you would specify a place_id of "5a110d312052166f"               
 * @param {string}  street_address   This parameter searches for places which have this given street address. There are other well-known, and application specific attributes available. Custom attributes are also permitted.
 * @param {string}  callback   If supplied, the response will use the JSONP format with a callback of the given name.
 * @methodhttp GET
 */
exports.geoSearch = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/geo/search.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'ip', 'str');
	url = url_replace(url, args, 'long', 'int');
	url = url_replace(url, args, 'max_results', 'int');
	url = url_replace(url, args, 'granularity', 'str');
	url = url_replace(url, args, 'lat', 'int');
	url = url_replace(url, args, 'accuracy', 'str');
	url = url_replace(url, args, 'contained_within', 'str');
	url = url_replace(url, args, 'street_address', 'str');
	url = url_replace(url, args, 'callback', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Locates places near the given coordinates which are similar in name.                   Conceptually you would use this method to get a list of known places to choose from first. Then, if the desired place doesn't exist, make a request to POST geo/place to create a new one.                   The token contained in the response is the token needed to be able to create a new place.
 * @category Geo
 * @param {string} [format=json]  undefined
 * @param {integer}  lat   The latitude to search around. This parameter will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding long parameter.
 * @param {integer}  long   The longitude to search around. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This parameter will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding lat parameter.
 * @param {string}  name   The name a place is known as.
 * @param {string}  contained_within   This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.                    Specify a place_id. For example, to scope all results to places within "San Francisco, CA USA", you would specify a place_id of "5a110d312052166f"               
 * @param {string}  street_address   This parameter searches for places which have this given street address. There are other well-known, and application specific attributes available. Custom attributes are also permitted.
 * @param {string}  callback   If supplied, the response will use the JSONP format with a callback of the given name.
 * @methodhttp GET
 */
exports.geoSimilarPlaces = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/geo/similar_places.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'lat', 'int');
	url = url_replace(url, args, 'long', 'int');
	url = url_replace(url, args, 'name', 'str');
	url = url_replace(url, args, 'contained_within', 'str');
	url = url_replace(url, args, 'street_address', 'str');
	url = url_replace(url, args, 'callback', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Given a latitude and a longitude, searches for up to 20 places that can be used as a place_id when updating a status.               This request is an informative call and will deliver generalized results about geography.
 * @category Geo
 * @param {string} [format=json]  undefined
 * @param {integer}  long   The longitude to search around. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This parameter will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding lat parameter.
 * @param {integer}  max_results   A hint as to the number of results to return. This does not guarantee that the number of results returned will equal max_results, but instead informs how many "nearby" results to return. Ideally, only pass in the number of places you intend to display to the user here.
 * @param {string}  granularity   This is the minimal granularity of place types to return and must be one of: poi, neighborhood, city, admin or country. If no granularity is provided for the request neighborhood is assumed.                Setting this to city, for example, will find places which have a type of city, admin or country.           
 * @param {integer}  lat   The latitude to search around. This parameter will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding long parameter.
 * @param {string}  accuracy   A hint on the "region" in which to search. If a number, then this is a radius in meters, but it can also take a string that is suffixed with ft to specify feet. If this is not passed in, then it is assumed to be 0m. If coming from a device, in practice, this value is whatever accuracy the device has measuring its location (whether it be coming from a GPS, WiFi triangulation, etc.).
 * @param {string}  callback   If supplied, the response will use the JSONP format with a callback of the given name.
 * @methodhttp GET
 */
exports.geocode = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/geo/reverse_geocode.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'long', 'int');
	url = url_replace(url, args, 'max_results', 'int');
	url = url_replace(url, args, 'granularity', 'str');
	url = url_replace(url, args, 'lat', 'int');
	url = url_replace(url, args, 'accuracy', 'str');
	url = url_replace(url, args, 'callback', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns all the information about a known place.
 * @category Geo
 * @param {string} [format=json]  undefined
 * @param {string} [place_id]  A place in the world. These IDs can be retrieved from geo/reverse_geocode.
 * @methodhttp GET
 */
exports.getPlaceInfo = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/geo/id/{place_id}.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'place_id', 'str', true);
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Creates a new place object at the given latitude and longitude.               Before creating a place you need to query GET geo/similar_places with the latitude, longitude and name of the place you wish to create. The query will return an array of places which are similar to the one you wish to create, and a token. If the place you wish to create isn't in the returned array you can use the token with this method to create a new one.
 * @category Geo
 * @param {string} [format=json]  undefined
 * @param {integer}  lat   The latitude the place is located at. This parameter will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding long parameter.
 * @param {integer}  long   The longitude the place is located at. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This parameter will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding lat parameter.
 * @param {string}  name   The name a place is known as.
 * @param {string}  token   The token found in the response from geo/similar_places.
 * @param {string}  contained_within   The place_id within which the new place can be found. Try and be as close as possible with the containing place. For example, for a room in a building, set the contained_within as the building place_id.
 * @param {string}  street_address   This parameter searches for places which have this given street address. There are other well-known, and application specific attributes available. Custom attributes are also permitted.
 * @param {string}  callback   If supplied, the response will use the JSONP format with a callback of the given name.
 * @methodhttp POST
 */
exports.createPlace = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/geo/place.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'lat', 'int');
	url = url_replace(url, args, 'long', 'int');
	url = url_replace(url, args, 'name', 'str');
	url = url_replace(url, args, 'token', 'str');
	url = url_replace(url, args, 'contained_within', 'str');
	url = url_replace(url, args, 'street_address', 'str');
	url = url_replace(url, args, 'callback', 'str');
	return http.post({url:url, body:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the current configuration used by Twitter including twitter.com slugs which are not usernames, maximum photo resolutions, and t.co URL lengths.               It is recommended applications request this endpoint when they are loaded, but no more than once a day.
 * @category Help
 * @param {string} [format=json]  undefined
 * @methodhttp GET
 */
exports.configuration = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/help/configuration.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the list of languages supported by Twitter along with their ISO 639-1 code. The ISO 639-1 code is the two letter value to use if you include lang with any of your requests.
 * @category Help
 * @param {string} [format=json]  undefined
 * @methodhttp GET
 */
exports.getLanguages = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/help/languages.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the Twitter Terms of Service in the requested format. These are not the same as the Developer Rules of the Road.
 * @category Legal
 * @param {string} [format=json]  undefined
 * @methodhttp GET
 */
exports.termsOfService = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/help/tos.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns the current rate limits for methods belonging to the specified resource families.Each 1.1 API resource belongs to a "resource family" which is indicated in its method documentation. You can typically determine a method's resource family from the first component of the path after the resource version.This method responds with a map of methods belonging to the families specified by the resources parameter, the current remaining uses for each of those resources within the current rate limiting window, and its expiration time in epoch time. It also includes a rate_limit_context field that indicates the current access token or application-only authentication context.You may also issue requests to this method without any parameters to receive a map of all rate limited GET methods. If your application only uses a few of methods, please explicitly provide a resources parameter with the specified resource families you work with.When using app-only auth, this method's response indicates the app-only auth rate limiting context.
 * @category Help
 * @param {string} [format=json]  undefined
 * @param {string}  resources   A comma-separated list of resource families you want to know the current rate limit disposition for. For best performance, only specify the resource families pertinent to your application.See Rate Limiting in API v1.1 for more information.Example Values: statuses,friends,trends,help           
 * @methodhttp GET
 */
exports.getRateLimitStatus = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/application/rate_limit_status.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'resources', 'str');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns Twitter's Privacy Policy.
 * @category Legal
 * @param {string} [format=json]  undefined
 * @methodhttp GET
 */
exports.privacyPolicy = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/help/privacy.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a cursored collection of user IDs for every user the specified user is following (otherwise known as their "friends").                   At this time, results are ordered with the most recent following first — however, this ordering is subject to unannounced change and eventual consistency issues. Results are given in groups of 5,000 user IDs and multiple "pages" of results can be navigated through using the next_cursor value in subsequent requests. See Using cursors to navigate collections for more information.                   This method is especially powerful when used in conjunction with GET users/lookup, a method that allows you to convert user IDs into full user objects in bulk.
 * @category Friendship
 * @param {string} [format=json]  undefined
 * @param {integer}  user_id   The ID of the user for whom to return results for.
 * @param {string}  screen_name   The screen name of the user for whom to return results for.
 * @param {integer}  cursor   Causes the list of connections to be broken into pages of no more than 5000 IDs at a time. The number of IDs returned is not guaranteed to be 5000 as suspended users are filtered out after connections are queried. If no cursor is provided, a value of -1 will be assumed, which is the first "page."                The response from the API will include a previous_cursor and next_cursor to allow paging back and forth.
 * @param {boolean}  stringify_ids   Many programming environments will not consume our Tweet ids due to their size. Provide this option to have ids returned as strings instead.
 * @methodhttp GET
 */
exports.friendsIds = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/friends/ids.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'user_id', 'int');
	url = url_replace(url, args, 'screen_name', 'str');
	url = url_replace(url, args, 'cursor', 'int');
	url = url_replace(url, args, 'stringify_ids');
	return http({url:url, params:args, auth:'oauth1', provider:provider}, opts);
};

/** 
 * Returns a collection of relevant Tweets matching a specified query.Please note that Twitter's search service and, by extension, the Search API is not meant to be an exhaustive source of Tweets. Not all Tweets will be indexed or made available via the search interface.In API v1.1, the response format of the Search API has been improved to return Tweet objects more similar to the objects you'll find across the REST API and platform. You may need to tolerate some inconsistencies and variance in perspectival values (fields that pertain to the perspective of the authenticating user) and embedded user objects.To learn how to use Twitter Search effectively, consult our guide to Using the Twitter Search API. See Working with Timelines to learn best practices for navigating results by since_id and max_id.              
 * @category Search
 * @param {string} [format=json]  undefined
 * @param {string} [q]  undefined
 * @param {string}  callback   If supplied, the response will use the JSONP format with a callback of the given name. The usefulness of this parameter is somewhat diminished by the requirement of authentication for requests to this endpoint.Example Values: processTweets
 * @param {string}  geocode   Returns tweets by users located within a given radius of the given latitude/longitude. The location is preferentially taking from the Geotagging API, but will fall back to their Twitter profile. The parameter value is specified by "latitude,longitude,radius", where radius units must be specified as either "mi" (miles) or "km" (kilometers). Note that you cannot use the near operator via the API to geocode arbitrary locations; however you can use this geocode parameter to search near geocodes directly. A maximum of 1,000 distinct "sub-regions" will be considered when using the radius modifier.Example Values: 37.781157,-122.398720,1mi
 * @param {string}  lang   Restricts tweets to the given language, given by an ISO 639-1 code. Language detection is best-effort.Example Values: eu
 * @param {string}  locale   Specify the language of the query you are sending (only ja is currently effective). This is intended for language-specific consumers and the default should work in the majority of cases.Example Values: ja
 * @param {string}  count   The number of tweets to return per page, up to a maximum of 100. Defaults to 15. This was formerly the "rpp" parameter in the old Search API.Example Values: 100
 * @param {string}  result_type   Optional. Specifies what type of search results you would prefer to receive. The current default is "mixed." Valid values include:  * mixed: Include both popular and real time results in the response.  * recent: return only the most recent results in the response  * popular: return only the most popular results in the response.Example Values: mixed, recent, popular
 * @param {string}  max_id   Returns results with an ID less than (that is, older than) or equal to the specified ID.Example Values: 54321
 * @param {string}  until   Returns tweets generated before the given date. Date should be formatted as YYYY-MM-DD. Keep in mind that the search index may not go back as far as the date you specify here.Example Values: 2012-09-01
 * @param {string}  since_id   Returns results with an ID greater than (that is, more recent than) the specified ID. There are limits to the number of Tweets which can be accessed through the API. If the limit of Tweets has occured since the since_id, the since_id will be forced to the oldest ID available.Example Values: 12345
 * @param {string}  include_entities   The entities node will be disincluded when set to false.Example Values: false
 * @methodhttp GET
 */
exports.search = function(args, opts) {
	var url = 'https://api.twitter.com/1.1/search/tweets.{format}';
	args = args || {};
	url = url_replace(url, args, 'format', 'str', true, "json");
	url = url_replace(url, args, 'q', 'str', true);
	url = url_replace(url, args, 'callback', 'str');
	url = url_replace(url, args, 'geocode', 'str');
	url = url_replace(url, args, 'lang', 'str');
	url = url_replace(url, args, 'locale', 'str');
	url = url_replace(url, args, 'count', 'str');
	url = url_replace(url, args, 'result_type', 'str');
	url = url_replace(url, args, 'max_id', 'str');
	url = url_replace(url, args, 'until', 'str');
	url = url_replace(url, args, 'since_id', 'str');
	url = url_replace(url, args, 'include_entities', 'str');
	return http({url:url, params:args, provider:provider}, opts);
};